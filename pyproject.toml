[project]
name = "git-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BytemanD", email = "byteman.d@outlook.com" }
]
requires-python = ">=3.10.0"
dependencies = [
    "click>=8.3.1",
    "gitpython>=3.1.46",
    "rich>=14.3.3",
]

[project.scripts]
git-stats = "git_stats.main:app"

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
